# Каждый пуш в main собирает проект и публикует его на GitLab Pages
pages:
  image: node:22
  script:
    - npm ci
    - npm run build
    - cp -r dist public
  artifacts:
    paths:
      - public
  only:
    - main
